name: mining_profit
description: Flutter app to track profit
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^4.0.4
  get_it: ^7.2.0
  auto_route: ^3.2.0
  json_serializable: ^6.1.3
  equatable: ^2.0.3
  hive: ^2.0.5
  hive_flutter: ^1.1.0
  local_auth: ^1.1.9
  flutter_masked_text2: ^0.9.1
  flutter_secure_storage: ^5.0.2
  intl: ^0.17.0
  flutter_bloc: ^8.0.1
  charts_flutter: ^0.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  auto_route_generator: ^3.2.0
  build_runner: ^2.1.7
  flutter_lints: ^1.0.4

flutter:
  uses-material-design: true
